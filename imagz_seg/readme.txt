Fichier pour les codes de segmentation d'images, création de maillage.

Le répertoire "mesh testing is done here" est le répertoire active de testing du programme mesh.edp, "working tested directory" est celui du traitement d'image, le troisième est laissé pour le produit final. le fichier mesh.edp, launcher.sh sont à voir cette fois pour le maillage. Test.cpp aussi pour voir comment extraire les données de traitement pour reconstruire l'image en FreeFam++.
